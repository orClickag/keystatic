---
import type { CtaData } from './CtaData';

type Props = CtaData;
const { title, description, image } = Astro.props;
---

<section class="cta">
  {image?.src ? <img src={image.src} alt={image.alt ?? ''} /> : null}
  <h2>{title}</h2>
  {description ? <p>{description}</p> : null}
</section>
